---
interface Props {
  id?: string;
  className?: string;
  spaced?: boolean;
}

const { id, className = "", spaced = true } = Astro.props;
---

<section id={id} class:list={[spaced && "section-space", className]}>
  <div class="container-shell">
    <slot />
  </div>
</section>
